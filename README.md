# mini-os 
- mini-os is a lightweight real-time operating system for mini-tree this project is mini-os's kernel

## features
- mini-os kernel included thread(task) list queue time memory scheduler and adapt to mini-tree its default not include standard -c library
